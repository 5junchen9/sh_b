# Q4 已有策略 Pareto 敏感性代码复审

> **状态：passed_with_warnings**  
> **复审脚本：** `code/Q4/q4_existing_policy_sensitivity.py`

## 通过项

1. ✅ 第 22 行输入仅引用 Q4 已有策略汇总表，未读取 Secondary、原始 MAT 或任何新 pilot 数据。
2. ✅ 第 31–40 行的 Pareto 判定保持“时间不更慢、寿命不更低且至少一项严格改善”的方向，与 Q4 主目标一致。
3. ✅ 第 43–49 行每个情景先重算非支配集，再用策略文本集合计算与基线的保留率，未把基线标签直接复制到敏感性结果。
4. ✅ 第 57–60 行明确比较 n≥2 与 n≥3；实际数据中基线 4 点均为 n=2，收紧阈值后保留率为 0%，与汇总表的样本量逐条一致。
5. ✅ 第 66–71 行对基线 Pareto 的每一枚电芯做留一资格检查；因为每点仅 n=2，移除任一枚即降为单电芯案例，脚本将该机制显式写入输出而非掩盖。
6. ✅ 第 76–84 行将情景表、留一表、中文 PNG 和 SVG 全部存入 `robustness/Q4/`，脚本不覆写上游 Q4 实验表。
7. ✅ 脚本通过 `python -m py_compile` 与实际运行；生成的情景表给出 baseline=4、n≥3 保留 0%、中位数口径保留 75%、来源平衡保留 100%、留一资格保持 0%。

## 约束方向复核

| 位置 | 方向/条件 | 含义 |
|---|---|---|
| 第 35 行 | `time <= time_i` | 支配策略不能更慢。 |
| 第 35 行 | `life >= life_i` | 支配策略不能有更低保守寿命摘要。 |
| 第 57 行 | `cell_count >= 3` | 样本阈值收紧情景，不是新的正式规则。 |
| 第 68 行 | `remaining >= 2` | 留一后仍满足已有策略 Pareto 的最低样本资格。 |

## 剩余风险

- 敏感性结果反映策略样本数与寿命摘要口径的影响，不包括代理模型重拟合或独立批次不确定性。
- 0% 的 n≥3 与留一保留率说明现有 4 点只能作为探索性开发池案例；它并不否定 Q2/Q3 的预测工件，也不能替代新策略 pilot。

## 运行方式

```powershell
.\.venv\Scripts\python.exe l1\code\Q4\q4_existing_policy_sensitivity.py
```

## 下一步

将 Q4 已有策略报告、敏感性报告和冻结 pilot 协议合并为完整 Q4 结果分析：正式区分“已有策略的探索性 Pareto 案例”“新策略 Q2_provisional”与“尚未取得的 Q3_confirmed”。
