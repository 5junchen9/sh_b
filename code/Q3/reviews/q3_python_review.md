# Q3 Python 代码复审

> **状态**：passed_with_warnings  
> **对象**：`code/Q3/q3_run_all.py`  
> **脚本 SHA-256**：`871773789ea26952ebee16a6895ca00aa4562d3a386279d1c3591333123388cb`

## 明确通过项

1. ✅ `q3_run_all.py:45-49` 要求 P0 通过；五个窗口均只保留 Train 的 41 枚电芯。
2. ✅ `:27-31, :49-52` 使用策略分组嵌套验证；填补、标准化和 alpha 选择全部只在相应训练折完成。
3. ✅ `:33-42` 的单调 SOH 模板只使用外层训练电芯及其寿命，不读取测试折寿命或未来 SOH 建模板。
4. ✅ `:22-24` 显式解析布尔字段；字符串 `False` 不会被误当作真。
5. ✅ 寿命 OOF 表共 205 行（5×41），保存 `outer_fold`，每个窗口同一策略只属于一个外层折。
6. ✅ `:54-63` 分开记录四类曲线失败原因；五个窗口均 41/41 成功、四类失败均为 0。
7. ✅ `:60, :64` 保存逐电芯逐窗口 SOH 误差表，共 205 行；可由 `soh_mse×future_point_count` 精确重建 pooled 指标。
8. ✅ `:68` 保存脚本、P0、标签、循环视图及五套早期特征哈希；全部与当前文件匹配。
9. ✅ 目标明确为 `ln(L)`、逆变换为 `exp`；基础 `future_point_pooled_soh_rmse` 已标为观测点合并诊断，论文正式比较改用稳健性报告的电芯等权指标。
10. ✅ 已执行 `py_compile` 与完整 `-W error` 运行；五窗口寿命误差、中文图、日志和摘要均可复现。

## 剩余边界

- Train 规模和策略重复数很小，窗口差异必须带 bootstrap 区间报告。
- `k=5` 是早期筛查，不等于仅凭 4 个有效循环点即可物理确定寿命；`k=100` 的正式角色来自人工记录 `Q3-D02`。
- 外部泛化、Primary 受限确认和新策略 pilot 均尚未完成。

## 复现命令

```powershell
.\.venv\Scripts\python.exe -W error l1\code\Q3\q3_run_all.py
```
