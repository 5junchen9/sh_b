# 中文图表重绘记录

**日期：**2026-08-02  
**范围：**`results/Q1`、`results/Q2`、`results/Q3` 的全部现有 PNG 图。

## 已执行变更

- Q1 六张、Q2 一张、Q3 一张图的标题、坐标轴、图例和图内解释均改为简体中文；变量符号与标准评价指标（如 `C1`、`SOC`、`SOH`、`RMSE`）保留原记号。
- 作图脚本统一在导入 `matplotlib.pyplot` 前设置 `Agg` 后端和中文字体序列（微软雅黑、黑体、DejaVu Sans），并关闭 Unicode 负号替换。
- 重跑 Q1、Q2、Q3 全部脚本。图表数据、模型和评价指标未因本次文字本地化发生改变。

## 验证

- `python -W error l1/code/Q1/q1_baseline.py`：通过，输出 6 张图。
- `python -W error l1/code/Q2/q2_run_all.py`：通过。
- `python -W error l1/code/Q3/q3_run_all.py`：通过。
- 已逐张目视检查全部 8 张 PNG，无中文缺字或英文业务说明残留。

后续新增图表必须遵循 [绘图中文化约定](绘图中文化约定.md)。
