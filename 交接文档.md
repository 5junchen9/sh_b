# L1 交接文档

更新日期：2026-08-02

## 1. 项目状态

L1 已完成原始数据盘点、论文名册对齐、MAT 深层审计、P0 正式建模视图、Q1 描述性分析、Q2/Q3 Train-only 正式验证与稳健性，以及 Q4 Train-only 候选流量 dry-run。

P0 数据门禁已通过：99,279 行寿命前循环、124 枚电芯、七字段逐循环掩码、五套早期窗口均已机器校验。P0、Q1、Q2-A、Q2-B、Q3、稳健性和 Q4 dry-run 脚本均已在 `-W error` 下按最新版本复跑；P0 最终重建后 Q2-B/Q4 也已追溯重跑，脚本、输入与运行摘要 SHA-256 一致，关键数值未变化。Q2-B 已内置 Windows joblib 核心探测兼容处理，交接者无需额外设置环境变量。

当前已停止 Q2/Q3 本轮模型迭代：Q2 为限定范围内中等可信，Q3 的双窗口范围已冻结但稳定性置信尚未单独裁决。Q4 的 1775 条输出全部是 `Q2_provisional`，可按 k=100 pilot 协议收集新策略真实早期数据，但没有形成正式 Pareto 或最优策略。基于 MAT 原始 `cycles.t/Qc/I/V/T/Qd` 的曲线特征仍必须逐循环应用深层审计掩码。

P0 正式状态见 `data/processed/p0_summary.json`：`p0_status=pass`。旧的 `data/processed/early_cycle_features.csv` 仅为探索性历史产物，不得替代正式五套窗口特征。

### 1.1 当前建模状态

| 模块 | 当前结果 | 状态边界 |
|---|---|---|
| Q1 | 9 张中文图、7 张表 | 描述性，不作因果结论 |
| Q2-A | M1 `RMSE_log/MAE_log=0.37169/0.27557`；M2 `0.36854/0.25102` | `Q2-D02`：M1 正文、M2 敏感性；结果/稳定性置信待裁决 |
| Q2-B | P3 Train `0.34892/0.23383`；Primary `0.28927/0.22572`；C1 Train `0.33168/0.22735` | `Q2-D03/D04`：P3 已受限确认，仅作 Q4 provisional 候选代理；C1 替代区间跨 0；稳定性置信待裁决 |
| Q3 | Train：k=5 `0.27936/0.03817`、k=100 `0.23174/0.03475`；Primary：k=5 `0.30305/0.04259`、k=100 `0.39702/0.03843` | `Q3-D02/D03`：k=5 筛查、k=100 正式且待外部确认；Primary 是受限观察，不能重选窗口或替代 Secondary；稳定性置信待裁决 |
| Q4 | `3653→2858→2398→2329→1775`；15 条排程 Pareto、3 条代表策略 | 已形成最小 9 枚电芯的 k=100 pilot 排程；1775 条及 3 条代表点均为 provisional，不是最终推荐 |

上述 `RMSE_log/MAE_log` 均为自然对数 `ln(L)` 尺度；总方案中旧 `log10` 公式已统一修正为 `ln/exp`。

## 2. 数据与标签规则

- 原始附件：`data_1.mat`、`data_2.mat`、`data_3.mat`，只读；Excel 文件为审计导出件，不替代 MAT。
- 正式分析单位：124 个唯一物理电芯条码。
- 正式寿命、数据分区、C1/Q1/C2：只使用论文 `1710560890-r2I8.pdf` 的 Supplementary Table 9。
- 5 个跨批次续测条码按 MAT 的真实批次日期拼接：`data_1 (2017-05-12) → data_2 (2017-06-30)`。
- 11 个论文名册外条码不进入主分析，但原始数据保留。
- 任意训练/验证/测试切分均以 `barcode` 分组，禁止同一条码跨集合。

原始 MAT 的 SHA-256 已记录于 `outputs/data_audit/inventory.json`；交接后首次运行前应复核哈希。

## 3. 主要产物

| 文件 | 用途 |
|---|---|
| `data/processed/cell_labels.csv` | 124 个电芯的正式标签、分区和策略 |
| `data/processed/cycle_summary_clean.csv` | 100,501 行循环汇总；保留来源片段、原始与全局循环索引、异常标记 |
| `data/processed/early_cycle_features.csv` | 124 行前 100 循环候选特征；不含寿命标签与数据分区 |
| `data/processed/feature_columns.json` | 特征白名单、标签表、目标列和分区列，建模必须显式读取 |
| `data/processed/data_preparation_summary.json` | 可机读整理摘要和剩余风险 |
| `data/processed/cycle_model_view.csv` | P0 唯一正式长表；99,279 行、七字段值/有效标志/原因、双 SOH、RAW 可用标志 |
| `data/processed/early_features_k5.csv` | 截止 cycle 5 的正式特征，124 行 |
| `data/processed/early_features_k10.csv` | 截止 cycle 10 的正式特征，124 行 |
| `data/processed/early_features_k20.csv` | 截止 cycle 20 的正式特征，124 行 |
| `data/processed/early_features_k50.csv` | 截止 cycle 50 的正式特征，124 行 |
| `data/processed/early_features_k100.csv` | 截止 cycle 100 的正式特征，124 行 |
| `data/processed/p0_summary.json` | P0 阈值、输入/脚本/输出哈希、掩码计数和机器检查 |
| `data/p0_audit_report.md` | P0 人工可读审计结论 |
| `code/P0/reviews/p0_python_review.md` | P0 Python 实现复核、修复项和运行说明 |
| `paper/data_processing_and_split_details.md` | 论文可用的数据处理、三类重复/排除、分区关系和数字追溯底稿 |
| `data/data_report.md` | 数据整理规则、异常处理与适用边界 |
| `outputs/data_audit/data_audit_report.md` | 文件、名册、Table 9 对齐审计 |
| `outputs/data_audit/table9_reconciliation.csv` | 本地记录与论文 Table 9 的逐条对账 |
| `outputs/data_audit/mat_metadata.json` | 三份 MAT 的批次日期和代表性嵌套字段证据 |
| `outputs/data_audit/mat_deep_cycle_flags.csv` | 全部 114,738 个原始循环的字段—循环掩码 |
| `data/deep_mat_audit_report.md` | 深层数组审计规则、异常计数和使用方式 |
| `src/reviews/l1_deep_mat_audit_review.md` | 深层审计的独立代码与结果复核 |
| `src/reviews/l1_python_review_after_fixes.md` | 既有的 Python 代码审查结论 |
| `planning/symbol_table.md` | Q1–Q4 统一符号、单位、自然对数与 SOH 指标口径 |
| `planning/model_assumptions.md` | 已人工确认的统一假设；Q3 研究命题与 Q3/Q4 规则已分离 |
| `results/Q1/experiments/round1/q1_experiment_report_round1.md` | Q1 描述性结果与 8 项运行核验 |
| `results/Q2/experiments/round1/q2_experiment_report_round1.md` | Q2-A/Q2-B 标准实验报告 |
| `robustness/Q2/q2_robustness_report.md` | Q2 2000 次策略组块 bootstrap 与交互符号稳定性 |
| `methods/Q2/q2b_primary_confirmation_protocol.md` | P3 冻结后 Primary 一次受限确认的边界、参数和禁止项 |
| `results/Q2/experiments/q2b_primary_confirmation_round1/q2b_primary_confirmation_report.md` | P3 在 43 枚 Primary 电芯上的确认观察，不等于自动通过 |
| `code/Q2/reviews/q2b_primary_confirmation_python_review.md` | Primary 确认脚本的独立代码复审 |
| `results/Q3/experiments/round1/q3_experiment_report_round1.md` | Q3 五窗口标准实验报告 |
| `robustness/Q3/q3_robustness_report.md` | Q3 电芯等权区间与窗口 Pareto |
| `methods/Q3/q3_primary_confirmation_protocol.md` | 冻结 M3 的 Primary 一次受限确认边界、参数和禁止项 |
| `results/Q3/experiments/primary_confirmation_round1/q3_primary_confirmation_report.md` | k=5/100 在 43 枚 Primary 电芯上的确认观察，不等于独立外部验证 |
| `code/Q3/reviews/q3_primary_confirmation_python_review.md` | Q3 Primary 确认脚本的独立代码复审 |
| `results/Q4/experiments/train_dry_run_round1/q4_train_only_dry_run_report.md` | Q4 候选流量与可信域 dry-run |
| `methods/Q4/q4_k100_pilot_protocol.md` | 新策略 k=100 pilot 的冻结输入、最小复现、P0/Q3 检查与升级边界 |
| `data/pilot/q4_k100_pilot_registry_template.csv` | 新策略 pilot 的空登记表模板；填入真实数据后才可运行确认 |
| `results/Q4/experiments/pilot_design_round1/q4_pilot_batch_design_report.md` | 15 条时间—P3 点预测非支配候选中选出的 3 条排程代表点；仅供 pilot |
| `results/Q4/experiments/pilot_design_round1/tables/q4_k100_pilot_allocation_template.csv` | 3 条策略展开的 9 个空条码物理电芯槽位；包括 k=5 筛查和 k=100 确认状态列 |
| `code/Q4/q4_validate_pilot_registry.py` | 填入真实条码前后的 9 槽位、复现数、策略冻结与 k=5/k=100 状态门禁 |
| `results/Q4/experiments/pilot_registry_preflight_round1/run_summary.json` | 当前空槽位登记结构的预检结果；不表示 pilot 已执行 |
| `code/Q4/reviews/q4_pilot_batch_design_python_review.md` | pilot 排程脚本的独立复审与严格运行核验 |
| `paper/model_selection_early_warning_and_q4_details.md` | 论文写作用方法选择、窗口与 Q4 细节 |
| `planning/progress_dashboard.md` | 当前门禁、阻断与下一技能的唯一进度面板 |

## 4. 已处理异常

- 41 个七字段全零循环：原始值保留；在 P0 长表中七个对应字段分别标记无效，原因均为 `all_zero_placeholder`。
- 14 个充电时间远端异常：使用 Train 内拟合的 Q99×5 冻结上界 `90.050737`；只将对应字段—循环的 `valid_chargetime` 设为假，不影响同一行其他字段，也不删除电芯。
- P0 截断后的 RAW 不可用循环为 249 个；RAW 全量正式名册审计仍为 258 个，两者差异来自 EOL 截断。
- 5 个续测电芯：已用 `global_cycle_index` 连续拼接；原始 `cycle_index` 未修改。
- 本地策略/寿命与论文标签冲突：已冻结为论文 Table 9，不使用本地冲突字段作为正式标签。

## 5. 可复现命令

在项目根目录执行：

```powershell
python l1/src/audit_l1_data.py
matlab -batch "run(fullfile(pwd,'l1','src','audit_mat_metadata.m'))"
python l1/src/compare_xlsx_to_table9.py
matlab -batch "run(fullfile(pwd,'l1','src','audit_mat_cycles_deep.m'))"
python l1/src/prepare_cycle_data.py
.\.venv\Scripts\python.exe -W error l1\src\build_p0_model_view.py
.\.venv\Scripts\python.exe -W error l1\code\Q1\q1_baseline.py
.\.venv\Scripts\python.exe -W error l1\code\Q1\q1_enhanced.py
.\.venv\Scripts\python.exe -W error l1\code\Q2\q2_run_all.py
.\.venv\Scripts\python.exe -W error l1\code\Q2\q2b_train_proxy_comparison.py
.\.venv\Scripts\python.exe -W error l1\code\Q3\q3_run_all.py
.\.venv\Scripts\python.exe -W error l1\code\robustness\q2_q3_train_bootstrap.py
.\.venv\Scripts\python.exe -W error l1\code\Q4\q4_train_only_dry_run.py
.\.venv\Scripts\python.exe -W error l1\code\Q2\q2b_primary_confirmation.py
.\.venv\Scripts\python.exe -W error l1\code\Q3\q3_primary_confirmation.py
.\.venv\Scripts\python.exe -W error l1\code\Q4\q4_pilot_batch_design.py
```

完整重建顺序：MAT 元数据/深审 → 数据整理 → P0 → Q1 baseline/enhanced → Q2-A/Q2-B 与 Q3 → Q2/Q3 稳健性 → Q4 dry-run → 冻结 P3 的 Primary 受限确认 → 冻结 M3 的 Primary 受限确认。若只复核当前模型，从 P0 开始即可；Q1 enhanced 必须晚于 Q1 baseline。

## 6. 后续工作建议

1. Q2 的结果与稳定性裁决已登记为限定范围内中等可信；Q3 的双窗口和冻结范围已登记，但其稳定性置信仍可由建模者另行补充，不得由 AI 自动替代。
2. `planning/model_assumptions.md` 已完成确认；若后续更换方法或评价口径，必须重新人工确认受影响条目。
3. P3 与 M3 的 Primary 受限确认均已执行；不得看结果后替换 P3、k=5/k=100、阈值或候选池。Q3 Primary 中 k=5 的寿命误差更低、k=100 的未来 SOH 误差更低，证明两层用途不同而非单一全面胜出。若要新增模型，只能回到 Train 新开轮次；Secondary 保留为真正独立压力测试。
4. Q4 不得从 1775 条 provisional 中直接宣布“最优”；下一步按 `methods/Q4/q4_k100_pilot_protocol.md` 为每条拟试策略配置至少 3 枚不同电芯，先在 k=5 生成早筛记录，再运行至 k=100 生成冻结 Q3 正式确认。k=5 只用于预警/调度，不得单独升级或淘汰策略。正式结果仍需保守寿命下界、Q3 确认和 Secondary 压力测试。
5. MAT 曲线特征继续使用 `mat_deep_cycle_flags.csv` 的逐循环掩码；不可对时间 `t` 单独排序，也不可因局部异常删整芯。
6. 每次修改代码后重跑相应审查、稳健性、跨介质一致性和本交接文档。

## 7. 代码审查技能

Superpowers 的代码审查技能已放入项目自动发现目录：`.codex/skills/requesting-code-review/`。

它包含 `SKILL.md` 和 `code-reviewer.md`。下一次新任务会自动发现该技能；它要求在重大实现完成或合并前发起独立代码审查。
